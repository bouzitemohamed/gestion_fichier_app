service: my-service

provider:
  name: aws
  runtime: nodejs18.x

functions:
  health:
    handler: src/handler.health
    events:
      - httpApi:
          path: /api/health
          method: GET

plugins:
  - serverless-offline